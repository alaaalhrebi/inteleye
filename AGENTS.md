# IntelEye — AGENTS.md

## الغرض من الملف

يحدد هذا الملف التعليمات العامة على مستوى المستودع لأي وكيل برمجي يعمل على مشروع IntelEye.
يمكن لأي ملف `AGENTS.md` أكثر تخصيصًا داخل مجلد فرعي أن يتجاوز هذه التعليمات داخل نطاق ذلك المجلد فقط.

IntelEye هو تطبيق SaaS عربي أولًا ويدعم RTL بشكل أساسي، ومخصص لمراقبة آراء العملاء والسمعة الرقمية، وعرض ملخصات الأداء، والتحليلات المدعومة بالذكاء الاصطناعي، والردود المقترحة، والتقارير الخاصة بالمنشأة وفروعها.


---

## 1. مصدر الحقيقة Source of Truth

استخدم الأولوية التالية عند تحديد السلوك الحالي للمنتج:

1. طلب المستخدم الصريح في المهمة الحالية.
2. الكود الحالي داخل المستودع.
3. Supabase migrations وRLS policies الحالية.
4. الاختبارات والأنواع Types الموجودة.
5. هذا الملف `AGENTS.md`.
6. التعليقات القديمة، أو المتطلبات السابقة، أو الميزات المستقبلية غير المنفذة.

لا تفترض أبدًا أن فكرة قديمة للمنتج تم تنفيذها فعليًا.

قبل أي تعديل:

- اقرأ الـ route أو component أو helper المرتبط بالمهمة بشكل كافٍ لفهم التدفق كاملًا.
- ابحث عن جميع المراجع المتعلقة بالـ route أو الجدول أو الحقل أو الصلاحية أو الخطة أو الحالة أو environment variable المتأثر.
- افحص التحقق من الصلاحيات على مستوى الواجهة وعلى مستوى الخادم/قاعدة البيانات معًا.
- نفذ أصغر تعديل مترابط يحل المطلوب.
- لا تعيد تصميم أجزاء مجاورة دون طلب صريح.

لا تخترع مسارات ملفات، أو أعمدة قاعدة بيانات، أو API routes، أو قواعد خطط، أو scripts دون التحقق من وجودها أولًا.

---

## 2. التقنيات المؤكدة في المشروع

المستودع الحالي يستخدم:

- Next.js 14 باستخدام App Router
- React 18
- TypeScript
- Tailwind CSS 3
- Supabase Auth وPostgreSQL
- `@supabase/ssr` لإدارة الجلسات على الخادم
- `@supabase/supabase-js`
- Framer Motion
- Lucide React icons
- Moyasar للدفع من جهة العميل
- n8n webhook لإنشاء التقارير المخصصة

لا تضف framework آخر، أو نظام authentication جديد، أو database abstraction، أو payment flow، أو UI system جديد إلا إذا كان ذلك مطلوبًا صراحة.

### أوامر npm الموجودة حاليًا

يحتوي `package.json` الحالي على:

- `npm run dev`
- `npm run build`
- `npm run start`
- `npm run lint`

يوجد ملف اختبار متعلق بالتقارير داخل المشروع، لكن لا يوجد حاليًا script عام باسم `test` داخل `package.json`.
لا تدّعِ أن `npm test` موجود ما لم تتم إضافته فعليًا.

---

## 3. المسارات الحالية التي يتعامل معها المستخدم

لا تصف أسطح المنتج بشكل عام إذا كان بالإمكان تحديد الـ routes الموجودة فعليًا.

### المسارات العامة والحساب والدفع

- `/` — الصفحة التعريفية العامة Landing Page
- `/pricing` — اختيار الباقة
- `/signup` — إنشاء الحساب وبدء تجربة Basic
- `/login` — تسجيل الدخول
- `/reset-password` — طلب إعادة تعيين كلمة المرور
- `/update-password` — تعيين/تحديث كلمة المرور
- `/auth/error` — واجهة أخطاء المصادقة
- `/auth/callback` — route handler الخاص بالمصادقة
- `/checkout` — الدفع عبر Moyasar
- `/checkout/success` — انتظار تفعيل الاشتراك بعد الدفع

### المسارات التشغيلية بعد تسجيل الدخول

- `/onboarding/platforms` — ربط منصة
- `/dashboard` — لوحة التحكم الرئيسية وملخص تحليلات السمعة
- `/dashboard/branches` — إدارة الفروع للمشتركين المدفوعين المصرح لهم
- `/dashboard/reports` — سجل التقارير وإدارة التقارير المخصصة للمشتركين المصرح لهم

### مسارات موجودة حاليًا كصفحات أولية Placeholder / Scaffold

هذه المسارات موجودة، لكن صفحاتها الحالية أولية وبسيطة، ويجب **عدم اعتبارها وحدات مكتملة** إلا إذا كانت المهمة الحالية تطورها بالفعل:

- `/dashboard/platforms`
- `/dashboard/replies`
- `/dashboard/settings`
تحتاج اضافة ازرار لها في dashboard
1. /dashboard/platforms — حالة المنصات
هذه الصفحة مخصصة لعرض نظرة عامة على حالة المنصات على مستوى حساب العميل بالكامل، وليست لإدارة أو ربط منصة كل فرع بشكل مباشر.
المنصات التي يتم عرضها حاليًا:
Google Maps
X
TikTok
Instagram
لكل منصة يجب عرض المعلومات التالية قدر الإمكان:
هل المنصة متاحة ومفعلة ضمن باقة العميل أم غير متاحة.
عدد الفروع المرتبطة بهذه المنصة.
آخر وقت تم فيه جلب أو مزامنة البيانات.
حالة الاتصال، مثل:
متصل.
يحتاج إلى إعادة ربط.
غير مرتبط.
عدد التعليقات أو الإشارات التي تم جمعها خلال الفترة الحالية.
وجود أخطاء أو مشاكل في المزامنة إن وجدت.
إمكانية الانتقال إلى الفروع المرتبطة بهذه المنصة.
زر مثل إدارة الربط أو عرض الفروع المرتبطة، على أن ينقل المستخدم إلى صفحة إدارة الفروع بدل تكرار إعدادات الربط داخل صفحة المنصات.
صفحة Platforms يجب أن تكون صفحة مراقبة لحالة المنصات وليست بديلًا عن صفحة Branches.
منطق حالة المنصة
لا تعتمد حالة المنصة على عامل واحد فقط، بل على عدة شروط، مثل:
المنصة مفعلة =
الاشتراك يسمح باستخدام المنصة
+ يوجد فرع واحد على الأقل مرتبط بها
+ بيانات الربط أو الاتصال صحيحة
إذا كانت باقة العميل لا تسمح باستخدام المنصة، تظهر حالة مثل:
غير متاحة في باقتك — ترقية الباقة
إذا كانت المنصة متاحة ضمن الباقة ولكن لم يتم ربطها بأي فرع، تظهر حالة مثل:
متاحة — لم يتم ربط أي فرع بعد
إذا كانت المنصة مرتبطة ولكن توجد مشكلة في بيانات الربط أو المزامنة، تظهر حالة واضحة مثل:
يحتاج إلى إعادة ربط
الهدف من هذه الصفحة أن تعمل كمركز لمراقبة حالة وتغطية وصحة المنصات على مستوى الحساب بالكامل.
2. /dashboard/replies — مركز الردود والمعالجة
هذه الصفحة هي مركز متابعة تعليقات وإشارات العملاء التي تحتاج إلى معالجة أو رد من جميع الفروع والمنصات في مكان واحد.
لا يتم نشر الرد مباشرة من Intel Eye في المرحلة الحالية. بدلاً من ذلك، يقوم النظام باقتراح الرد، ويقوم المستخدم بنسخه والرد من المنصة الأصلية.
يجب أن تعرض الصفحة التعليقات أو الإشارات التي تحتاج إلى متابعة، مثل السجلات التي يكون فيها:
needs_reply = true
ويفضل أن تظهر للمستخدم معلومات مثل:
فرع الياسمين
Google Maps

تعليق العميل:
"الخدمة كانت بطيئة جدًا والطلب وصل بارد"

التصنيف:
شكوى

المشاعر:
سلبية

الخطورة:
مرتفعة

الرد المقترح:
"نعتذر لك عن التجربة..."
يستخدم Intel Eye الذكاء الاصطناعي لاقتراح رد مناسب بناءً على عوامل مثل:
محتوى تعليق العميل.
المنصة.
التصنيف.
المشاعر sentiment.
درجة الخطورة severity.
نوع المشكلة أو الشكوى.
يستطيع المستخدم:
نسخ الرد المقترح.
الرد يدويًا من المنصة الأصلية.
تحديد الحالة على أنها تمت المعالجة بعد الانتهاء.
تجاهل الحالة إذا قرر أنها لا تحتاج إلى رد أو متابعة.
حالات المعالجة
يمكن استخدام حالات واضحة مثل:
يحتاج معالجة
تم نسخ الرد
تمت المعالجة
تم التجاهل
يجب عدم اعتبار تم نسخ الرد حالة نهائية؛ نسخ الرد لا يعني بالضرورة أن المستخدم نشره على المنصة.
الحالة النهائية الأساسية هي:
تمت المعالجة
ويكون التدفق المتوقع:
تعليق أو إشارة تحتاج إلى رد
        ↓
اقتراح رد بالذكاء الاصطناعي
        ↓
نسخ الرد
        ↓
المستخدم يرد من المنصة الأصلية
        ↓
تمت المعالجة
أما تم التجاهل فتستخدم عندما يقرر المستخدم أن الحالة لا تحتاج إلى معالجة.
3. /dashboard/settings — الإعدادات
هذه الصفحة هي المكان المركزي لإعدادات حساب Intel Eye.
يجب ألا تستخدم لإدارة الفروع أو ربط المنصات، لأن هذه الوظائف لها أماكن مخصصة داخل المشروع.
يمكن تقسيم صفحة الإعدادات إلى أقسام واضحة، مثل:
الحساب والمنشأة
يشمل المعلومات العامة المرتبطة بالحساب والمنشأة، مثل:
اسم المنشأة.
بيانات الحساب.
بيانات التواصل الأساسية.
أي معلومات عامة يحتاج المستخدم إلى إدارتها.
الاشتراك والفوترة
يعرض هذا القسم معلومات الاشتراك الحالية، مثل:
اسم الباقة.
حالة الاشتراك.
حالة الفترة التجريبية إن وجدت.
تاريخ انتهاء الفترة الحالية أو التجربة.
حدود عدد الفروع.
حدود عدد المنصات.
المزايا المتاحة ضمن الباقة.
الانتقال إلى الترقية أو إدارة الاشتراك عند الحاجة.


---

## 4. منصات التقييم والتغذية الراجعة الحالية

تدفق ربط المنصات وAPI الفروع يدعمان حاليًا مفاتيح المنصات التالية فقط:

- `google_maps` — Google Maps
- `x` — X
- `tiktok` — TikTok
- `instagram` — Instagram


السلوك الحالي للمدخلات:

- Google Maps يستخدم رابط الموقع.
- X يستخدم username ويتم توحيد القيمة بإزالة/معالجة `@` في البداية بشكل ثابت.
- TikTok يستخدم رابط الحساب.
- Instagram يستخدم رابط الحساب.
- إعداد المنصة يخزن أيضًا `business_activity`.

قد يستخدم النظام حاليًا صفوف منصات خاصة بفرع معين، أو صفوفًا عامة على مستوى المنشأة عندما تكون `branch_id = null`.
يجب الحفاظ على هذا الفرق.

---

## 5. سلوك لوحة التحكم الرئيسية

`/dashboard` هي الواجهة الأساسية لتحليل السمعة.

تقرأ حاليًا التقارير المكتملة أو حالات عدم وجود بيانات، وتعرض عناصر مثل:

- متوسط التقييم.
- عدد الآراء/المراجعات.
- نسبة الآراء الإيجابية والسلبية.
- الحالات العاجلة.
- عدد الردود المقترحة.
- أبرز المشكلات.
- التوصيات.
- المقارنات والاتجاهات الأسبوعية.
- التنبيهات الذكية.
- الردود المقترحة.
- معلومات المنصات المرتبطة.

قد تعتمد الفلاتر على query parameters مثل المنصة والفرع والفترة.

تحتوي لوحة التحكم أيضًا على روابط/إجراءات لإعداد المنصات والتقارير وإدارة الفروع عندما تسمح الصلاحيات بذلك.

لا تنشئ بنية تحليلات ثانية إذا كانت الميزة المطلوبة تنتمي بشكل طبيعي إلى نموذج dashboard/report الحالي.

---

## 6. المصادقة وحماية المسارات

استخدم helpers الحالية الخاصة بـ Supabase:

- `src/lib/supabase-browser.ts`
- `src/lib/supabase-server.ts`

الـ middleware الحالي يحمي المسارات التي تبدأ بـ:

- `/dashboard`
- `/onboarding`
- `/checkout`

المستخدم غير المسجل يتم تحويله إلى `/login`.
المستخدم المسجل الذي يزور `/login` أو `/signup` يتم تحويله إلى `/dashboard`.

لا تستخدم `localStorage` كدليل على أن المستخدم authenticated.
لا تنقل checks الخاصة بالمصادقة على الخادم أو الأسرار إلى Client Components.

### تدفق التسجيل Signup

التسجيلات الجديدة تمرر metadata تحتوي على `selected_plan: "basic"`، والواجهة تصف تجربة Basic لمدة 14 يومًا.
يشير كود التسجيل إلى function/trigger في قاعدة البيانات باسم `handle_new_client_signup`، لكن تعريف هذا trigger غير موجود في snapshot الحالي للمستودع.
لا تعيد كتابة أو تفترض تنفيذه دون فحص Supabase project/schema الفعلي عندما تكون هذه الوظيفة جزءًا من المهمة.

### سلوك PKCE وcallback

`/auth/callback` يدعم token-hash verification وPKCE code exchange ويحوّل أخطاء PKCE إلى أسباب واضحة في واجهة أخطاء المصادقة.
حافظ على هذا التدفق عند تعديل authentication.

---

## 7. صلاحيات الاشتراك حساسة للأعمال

الـ helper المركزي الحالي هو:

`src/lib/subscription-permissions.ts`

استخدمه بدل نشر status checks جديدة ومتكررة في مكونات الواجهة.

### السلوك الحالي في الكود

يمكن للعميل الوصول إلى لوحة التحكم إذا تحقق أحد الشرطين:

- `subscription_status === "trial"` و`trial_ends_at` ما زال في المستقبل.
- `subscription_status === "active"` وفترة الاشتراك المدفوعة ما زالت سارية.

السلوك الحالي للـ helper يعني أيضًا:

- trial سارية: يمكن الوصول إلى dashboard.
- trial سارية: يمكن استخدام منصة واحدة فقط.
- trial سارية: لا يسمح بإدارة الفروع.
- trial سارية: لا يسمح بالوصول إلى صفحة التقارير.
- اشتراك مدفوع active: يسمح بإدارة الفروع والوصول إلى سجل التقارير.
- إنشاء تقرير مخصص: متاح فقط لعملاء `pro` أو `enterprise` بحالة active.

لا "تصحح" هذه القواعد اعتمادًا على الذاكرة.
إذا طلب مالك المنتج تغيير سلوك trial، فحدّث helper وserver APIs وRLS/database functions والواجهة المتأثرة معًا.

### حدود الخطط الحالية داخل helper

حدود الفروع:

- `basic`: 1
- `pro`: 3
- `enterprise`: 20

حدود المنصات تعتمد أولًا على `clients.allowed_platforms_count` إذا كانت قيمة integer موجبة.
ويوجد fallback قديم حاليًا:

- `basic`: 1
- `pro`: 2
- `enterprise`: 4

في حالة trial فعالة، يتم فرض `platformLimit = 1`.

### وجود تعارضات حالية في تعريف الخطط

تفاصيل الخطط مكررة في أكثر من مكان وغير متطابقة بالكامل.
على سبيل المثال، `/pricing` و`src/components/Pricing/plans.ts` يحتويان على اختلافات في سعر/مزايا Pro، كما أن بعض حدود الفروع المعروضة لا تتطابق مع `subscription-permissions.ts`.

لذلك:

- ابحث عن جميع تعريفات الخطط قبل تعديل الأسعار أو الصلاحيات.
- لا تختَر قيمة من نسخة واحدة وتعدّل بقية المشروع تلقائيًا إلا إذا طلبت المهمة توحيدها صراحة.
- عند التحكم الفعلي في الوصول، أعطِ الأولوية لمنطق server/database على النص التسويقي داخل الواجهة.

---

## 8. نموذج بيانات Supabase الظاهر في المستودع الحالي

الكود الحالي يستعلم مباشرة من الجداول التالية:

- `clients`
- `branches`
- `client_platforms`
- `reports`
- `report_requests`

كما تشير migrations الحالية إلى بيانات feedback/analysis تشمل:

- `google_reviews`
- `x_mentions`
- `tiktok_comments`
- `instagram_comments`
- `feedback_analysis`
- view باسم `unified_feedback`

لا تفترض وجود جدول مستقل باسم `platforms` فقط لأن المنتج يحتوي على مفهوم المنصات.
تحقق من schema الفعلي قبل إضافة أي query.

### تعدد العملاء Multi-tenancy

عزل بيانات العملاء إلزامي.

نموذج الملكية الفعلي مبني حول مستخدم Supabase المسجل والصف المقابل له في جدول `clients`.
يجب أن تظل جميع البيانات الحساسة للمستأجر scoped إلى العميل الصحيح، وأن يتم التحقق من علاقات branch/platform قبل أي write.

لا تثق أبدًا في `client_id` قادم من المتصفح دون server-side authorization.

---

## 9. RLS ودوال الصلاحيات

تقوم migrations الحالية بإنشاء/نقل permission helpers إلى schema باسم `private` وتطبق RLS policies على الفروع، وربط المنصات، والتقارير، وطلبات التقارير، وجداول feedback.

المفاهيم ذات الصلة تشمل functions مكافئة لـ:

- `private.can_access_dashboard(...)`
- `private.can_manage_branches(...)`
- `private.can_access_custom_reports(...)`
- `private.can_add_branch(...)`
- `private.can_use_platform(...)`
- `private.can_create_custom_report(...)`

الـ view `unified_feedback` مضبوطة باستخدام `security_invoker = true`، والجداول الأساسية لديها tenant-aware select policies.

عند تغيير الصلاحيات افحص **الجانبين معًا**:

1. منطق الصلاحيات في TypeScript.
2. منطق Supabase migration/RLS.

لا تصلح الواجهة فقط وتترك authorization في قاعدة البيانات غير متطابق.
لا تعطل RLS كحل سريع.

---

## 10. إدارة الفروع

`/dashboard/branches` متاحة حاليًا فقط للمشتركين المدفوعين بحالة active وفق helper الصلاحيات المشتركة.

مسار الإنشاء على الخادم هو:

`POST /api/branches`

ويقوم حاليًا بـ:

- مصادقة المستخدم.
- تحميل client الخاص بالمستخدم.
- حساب عدد الفروع النشطة وعدد أسماء المنصات النشطة المختلفة.
- تطبيق subscription permissions.
- إنشاء الفرع.
- إنشاء ربط branch/platform.
- حذف الفرع الذي تم إنشاؤه للتو إذا فشل ربط المنصة.

عند تعديل إنشاء الفروع حافظ على سلوك cleanup شبه الذري هذا، إلا إذا تم استبداله بمعاملة database transaction/RPC حقيقية.

لا تسمح للواجهة بتجاوز checks الموجودة على الخادم لحدود الفروع أو المنصات.

---

## 11. ربط المنصات

واجهة ربط المنصات الأساسية المنفذة حاليًا هي `/onboarding/platforms`.

تقرأ العميل الحالي، وتتحقق من الصلاحيات، وتحسب أسماء المنصات النشطة المختلفة، ثم تضيف إلى `client_platforms`.

قواعد مهمة:

- طبّق حدود الاشتراك/المنصات في database/server إضافة إلى الواجهة.
- حافظ على اتساق `client_id` و`branch_id` الاختياري و`platform_name` والرابط/username و`business_activity` و`is_active`.
- حافظ على توحيد X usernames بالطريقة نفسها في جميع الأماكن.
- لا تعرض credentials/tokens الخاصة بالمنصات في logs أو output ظاهر للعميل.

`/dashboard/platforms` موجودة حاليًا كصفحة placeholder فقط؛ لا تفترض أنها تنفذ إدارة كاملة للربط.

---

## 12. التقارير

`/dashboard/reports` هي واجهة منفذة لإدارة التقارير.

السلوك الحالي يتضمن:

- تحميل التقارير وطلبات التقارير الموجودة.
- البحث والفلترة داخل سجل التقارير.
- عرض تفاصيل التقرير.
- polling عندما تكون الطلبات بحالة queued/pending/processing.
- السماح بإنشاء تقارير مخصصة فقط عندما تسمح الصلاحيات بذلك.

أنواع التقارير الحالية الظاهرة في الواجهة تشمل مفاهيم مثل:

- initial / historical initial
- weekly
- custom

### القيود الحالية للتقارير المخصصة

validator الحالي يدعم عمدًا فقط:

- نوع تقرير `custom`.
- اختيار فرع واحد فقط.
- اختيار منصة واحدة فقط لكل طلب.
- تاريخ بداية ونهاية صالحين.
- عدم السماح بتواريخ مستقبلية.
- حد أقصى لمدة التقرير شهر او ٣٠ يوم.
- عدم وجود اسم مخصص للتقرير.

لا توسع هذه القدرات بصمت دون تحديث validation والواجهة وn8n payload handling والاختبارات.

### API routes الحالية للتقارير

يوجد مساران POST:

- `/api/reports`
- `/api/reports/request`

بينهما تداخل لكنهما غير متطابقين.
بشكل خاص، `/api/reports` يحتوي على معالجة صريحة لمنصة عامة عندما `branch_id = null`، بينما `/api/reports/request` يستخدم مطابقة أكثر صرامة بين branch/platform.

قبل تعديل إرسال التقارير، ابحث في المسارين وحدد عن قصد ما إذا كان كلاهما يحتاج التحديث.
لا تسمح باختلافهما بشكل غير مقصود.

### n8n report webhook

الملف `src/lib/reports/n8n.ts` يرسل مهام التقارير المخصصة إلى n8n.

Environment variables المستخدمة:

- `N8N_REPORT_WEBHOOK_URL`
- `N8N_REPORT_WEBHOOK_SECRET`
- `N8N_REPORT_WEBHOOK_AUTH_HEADER` اختياري، والقيمة الافتراضية `Authorization`

التكامل الحالي:

- يتوقع HTTP `202` من n8n.
- يتوقع `request_id` غير فارغ.
- يحتوي على timeout/error mapping.
- لا يمرر الرد الخام من n8n إلى المستخدم عمدًا.

حافظ على خصائص الأمان ومعالجة الأخطاء هذه.

تعريفات workflows الفعلية الخاصة بـ n8n **غير موجودة في snapshot الحالي للمستودع**.
لا تدّعِ أن عقد ingestion أو report workflow منفذة داخل هذا المستودع ما لم يتم توفير ملفات n8n بشكل منفصل.

---

## 13. تدفق الدفع

`/checkout` يقوم حاليًا بتهيئة Moyasar داخل المتصفح باستخدام:

- `NEXT_PUBLIC_MOYASAR_PUBLISHABLE_KEY`

ويرسل checkout metadata تشمل `client_user_id` والخطة المختارة.

`/checkout/success` لا يقوم بتفعيل الاشتراك بنفسه.
بل يقوم بعمل polling على Supabase حتى تصبح `subscription_status` للعميل بقيمة `active`.

تعليقات الكود تشير إلى أن التفعيل الحقيقي يتم بواسطة server webhook، لكن تنفيذ هذا webhook غير موجود في snapshot الحالي للمستودع.

لذلك:

- لا تخترع payment webhook endpoint داخل المشروع إلا إذا طُلب ذلك صراحة.
- لا تغيّر الاشتراك إلى active لمجرد عودة المتصفح من Moyasar.
- حافظ على server-side payment verification كحد الثقة الأساسي.

---

## 14. بيانات الآراء والسمعة

تشير migrations الحالية إلى جداول خاصة بكل مصدر إضافة إلى `feedback_analysis` و`unified_feedback`.

حافظ على هوية المصدر والعميل لكل feedback record.
وعند توفر معرفات خارجية مستقرة، حافظ على idempotent ingestion لمنع التكرار.

إذا أضفت أو عدلت ingestion:

- امنع التكرارات.
- حافظ على اتساق timestamps.
- احتفظ بـ source metadata اللازمة للتشخيص.
- اجعل transitions الخاصة بحالة التحليل واضحة.
- لا تخلط بيانات العملاء أو الفروع.

snapshot الحالي للمستودع لا يحتوي على كامل ingestion workflows، لذلك افحص n8n workflows الخارجية قبل تغيير افتراضات الجدولة أو payloads الخاصة بالمصادر.

---

## 15. قواعد الواجهة والتصميم

IntelEye عربي أولًا وRTL أولًا.

حافظ على:

- النصوص العربية ما لم تطلب المهمة لغة أخرى.
- `dir="rtl"` ومحاذاة RTL.
- تجاوب التصميم مع الجوال.
- accessible labels وحالات الأزرار.
- أنماط components/layout الموجودة حاليًا.

### الاتجاه البصري الحالي

**لا تستخدم لوحة الألوان الخضراء القديمة كافتراضي للمشروع.**

التطبيق الحالي يستخدم بشكل متكرر ألوانًا مثل:

- `#374375` — أزرق/بنفسجي رئيسي للنصوص والإجراءات
- `#895159` — accent خمري هادئ
- `#BABDE2` — لافندر فاتح
- `#DFAEA1` — خوخي هادئ
- `#F8F7F3` / `#FFFCF5` — خلفيات دافئة
- `#1A1A2E` — لون داكن مستخدم في pricing/checkout

يحتوي `globals.css` أيضًا على root variables بألوان خمري/زيتوني، مما يعني وجود بعض عدم الاتساق في design tokens.

عند تعديل الواجهة:

- اتبع styles الصفحة/component المحيط.
- لا تعيد تصميم المنتج كاملًا بسبب تعديل محلي.
- لا تعيد إدخال لوحة الألوان الخضراء القديمة إلا بطلب صريح.
- إذا طُلب توحيد design system، افحص أولًا جميع تعريفات الألوان والـ tokens المكررة.

---

## 16. Server Components وClient Components

قبل إضافة `"use client"` تأكد أن component يحتاج فعليًا browser APIs أو state أو effects أو event handlers.

فضّل Server Components لتحميل بيانات المستخدم الموثق عندما يكون هذا هو النمط المستخدم في المشروع الحالي.

لا تنقل أسرار server-only إلى client code.

أعد استخدام utilities/components الحالية بدل إنشاء abstractions موازية دون حاجة.

---

## 17. الأمان والأسرار

لا تقم أبدًا بعمل commit أو كشف أي من التالي:

- Supabase service-role keys.
- أسرار `.env` الحقيقية.
- مفاتيح الدفع السرية.
- n8n webhook secrets.
- cookies/session tokens.
- OAuth/API tokens.
- credentials الخاصة بالمنصات.

Environment variables العامة/المسموح ظهورها للعميل تشمل قيم Supabase العامة وMoyasar publishable key.
أما الأسرار الخاصة بالخادم فيجب أن تبقى server-side.

احذف أو أخفِ authorization headers والأسرار من logs.

---

## 18. قواعد تغييرات قاعدة البيانات

عند تعديل schema أو authorization افحص جميع الطبقات المتأثرة:

1. Supabase migration / SQL
2. RLS policies وprivate permission functions
3. TypeScript entitlement helpers
4. server routes / server components
5. client UI
6. افتراضات n8n workflow الخارجية
7. report queries
8. tests

فضّل التغييرات الإضافية الآمنة والقابلة للـ migration.

لا تحذف tables أو columns أو policies أو constraints أو production data إلا إذا طلب المستخدم ذلك صراحة وكان الأثر مفهومًا.

---

## 19. معالجة الأخطاء

لا تخفِ الفشل خلف حالة نجاح.

في عمليات Supabase والدفع وn8n:

- ميّز بين authentication/authorization failures وvalidation failures وservice failures.
- اجعل رسائل المستخدم العربية واضحة ومفهومة.
- اجعل developer logs مفيدة لكن خالية من الأسرار.
- حافظ على timeout/unavailable-service handling الموجود حاليًا.

لا تعرض الرد الخام لخدمة خارجية للمستخدم لمجرد سهولة debugging.

---

## 20. الأجزاء غير المكتملة حاليًا

تعامل مع الأجزاء التالية على أنها غير مكتملة ما لم تطورها المهمة الحالية:

- واجهة الإدارة الكاملة لـ `/dashboard/platforms`.
- workflow الكامل لـ `/dashboard/replies`.
- الوظائف الكاملة لـ `/dashboard/settings`.
- تعريفات ingestion workflows الكاملة لـ Google Maps وX وTikTok وInstagram.
- payment webhook الذي يقوم بتفعيل الاشتراكات.
- تاريخ Supabase schema/triggers الكامل غير الظاهر في migrations الموجودة داخل snapshot الحالي.

لا تدّعِ اكتمال هذه الأجزاء فقط لأن route أو comment موجود.

---

## 21. تعارضات معروفة داخل المستودع

لا تقم بتوحيد هذه الأمور بصمت إلا إذا كانت المهمة تطلب ذلك:

- تعريفات الأسعار والمزايا مكررة وتختلف حاليًا بين `/pricing` و`src/components/Pricing/plans.ts`.
- عدد الفروع الظاهر في بعض الخطط قد يختلف عن entitlement helper.
- color/design tokens غير موحدة بالكامل.
- يوجد POST endpoints متداخلان للتقارير المخصصة مع اختلاف بسيط في قواعد branch/platform.

عندما تمس المهمة أحد هذه الأجزاء، ابحث في جميع النسخ وحدد السلوك المطلوب بشكل مقصود.

---

## 22. التحقق قبل إنهاء المهمة

في تعديلات الكود، شغّل checks الحالية ذات الصلة متى كان ذلك عمليًا:

- `npm run lint`
- `npm run build`

شغّل الاختبارات المستهدفة فقط باستخدام command موجود فعليًا أو مناسب للـ framework/file.
لا تدّعِ نجاح الاختبارات ما لم يتم تشغيلها.

عند تعديل auth/subscription، تحقق منطقيًا وعند الإمكان عبر الكود/الاختبارات من الحالات التالية:

- مستخدم غير مسجل.
- trial فعالة.
- trial منتهية أو مستخدم غير مستحق.
- Basic active.
- Pro active.
- Enterprise active.

في تعديلات الواجهة افحص سلوك desktop والجوال.

---

## 23. تقرير إنهاء المهمة

بعد الانتهاء من أي مهمة برمجية، اذكر باختصار:

1. ما الذي تغير.
2. الملفات التي تم تعديلها.
3. أثر التعديل على database/migrations.
4. environment variables الجديدة أو المعدلة.
5. خطوات التحقق التي تم تنفيذها.
6. أي شيء لم يمكن التحقق منه.

لا تعرض refactors غير مرتبطة بالمهمة على أنها إنجازات.

---

## 24. قائمة التحقق النهائية للوكيل

قبل إنهاء أي مهمة تأكد من التالي:

- [ ] فحصت التنفيذ الحالي الفعلي قبل التعديل.
- [ ] بحثت عن جميع المراجع المهمة التي تتأثر بالتغيير.
- [ ] حافظت على عزل بيانات العملاء Multi-tenancy.
- [ ] حافظت على تطابق subscription rules وRLS أو حدثتهما معًا بشكل مقصود.
- [ ] لم أكشف أي secrets.
- [ ] لم أفترض أن Salla/Zid منصات حالية؛ تحققت من platform keys الفعلية.
- [ ] حافظت على السلوك العربي وRTL.
- [ ] لم أتعامل مع placeholder routes كأنها modules مكتملة.
- [ ] تحققت من تعريفات pricing/report المكررة عندما تكون مرتبطة بالمهمة.
- [ ] استخدمت أنماط المشروع الحالية بدل اختراع بنية موازية.
- [ ] شغلت أوامر التحقق الموجودة ذات الصلة متى كان ذلك عمليًا.
- [ ] أوضحت أي شيء لم أتمكن من التحقق منه.
